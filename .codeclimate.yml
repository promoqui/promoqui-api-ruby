engines:
  fixme:
    enabled: true
  duplication:
    enabled: true
    config:
      languages:
        - ruby
        - javascript
  rubocop:
    enabled: true

exclude_paths:
  - spec/**/*

ratings:
  paths:
    - lib/**
    - Gemfile
